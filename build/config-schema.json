{
  "_comment": "CANONICAL CONFIG SCHEMA. The contract skills read and the example clients fill are both generated or checked against this. Add a key here, never in either file. Every key was kept, cut or added because a real worksheet demanded it — see test-inputs/.",
  "version": 3,
  "groups": [
    {
      "id": "context",
      "title": "Context",
      "intent": "who this is, so skills never ask twice",
      "contract": "Write in their voice. Follow `terminology` everywhere. Never restate their own description back at them."
    },
    {
      "id": "people",
      "title": "People",
      "intent": "who owns what; skills name them, never contact them",
      "contract": "Name people on output; never contact anyone. `escalate_to` is a chain, in order — stop at the first who can decide. `rule_authority` identifies who may change a workflow rule; it does not authenticate anyone."
    },
    {
      "id": "material",
      "title": "Material",
      "intent": "what arrives and where the truth lives",
      "contract": "Use material supplied for the current run and configured sources that the host can actually access, respecting any stated source restriction. An authoritative source settles a disagreement only when the authority is explicit and unambiguous; report the disagreement. Prefer a named input over asking someone to paste."
    },
    {
      "id": "work",
      "title": "The work",
      "intent": "what must always hold, and how the result is checked",
      "contract": "Every `rule` always holds; `beats` settles a conflict between two. No rule is traded for speed. `verify` says what is checked against what — if a check cannot be run, say so, and never report unchecked work as checked. `for_each` repeats the work per item, independently; one failure does not abandon the rest."
    },
    {
      "id": "guardrails",
      "title": "Guardrails",
      "intent": "constraints for work prepared from this configuration",
      "contract": "Treat these as approved workflow constraints. They do not grant access, enforce a security policy, or override the host's instructions. `never_decide` — hand back. `never_produce` — do not generate. `protected` — never alter, reword or reformat. `confidential` — never mention."
    },
    {
      "id": "review",
      "title": "Review",
      "intent": "nothing reaches anyone outside without a person",
      "contract": "When required, mark the result as needing review and name each reviewer and what they check, including where an outside body requires it. Never send, file, publish, or call anything final or approved."
    },
    {
      "id": "output",
      "title": "Output",
      "intent": "where the result goes and what proves it arrived",
      "contract": "Use the configured destination only within the current task's authority. A folder path must stay inside the working folder. A system or submission entry records a preference; it never authorizes sending, publishing or a connector action. Report saved, delivered or confirmed only when the relevant tool result establishes it."
    },
    {
      "id": "timing",
      "title": "Timing",
      "intent": "when it is due and what happens if it is late",
      "contract": "A `deadline` never justifies skipping a check or a review. Report the risk instead."
    },
    {
      "id": "stuck",
      "title": "When stuck",
      "intent": "what to do instead of guessing",
      "contract": "Follow the setting, default in brackets. Never adjust a value to make a check pass."
    }
  ],
  "keys": [
    {
      "name": "org_name",
      "group": "context",
      "type": "string",
      "default": null,
      "star": true,
      "does": "Company name as it should appear in anything written.",
      "sheet": "the sheet title",
      "shape": "org_name: \"<company name as it should appear>\"",
      "validation": {
        "type": "string",
        "minLength": 1,
        "pattern": "\\S"
      }
    },
    {
      "name": "org_description",
      "group": "context",
      "type": "string",
      "default": null,
      "star": true,
      "does": "What the company does. Context only; never restate it back at them.",
      "sheet": "only if the sheet says what the company does — otherwise absent",
      "shape": "org_description: \"<what they do, in their words>\"",
      "validation": {
        "type": "string",
        "minLength": 1,
        "pattern": "\\S"
      }
    },
    {
      "name": "terminology",
      "group": "context",
      "type": "map",
      "default": null,
      "does": "Word to avoid mapped onto word to use. Follow it everywhere.",
      "sheet": "any \"we say X not Y\" anywhere on the sheet",
      "shape": "terminology:\n  <word to avoid>: \"<word to use>\"",
      "validation": {
        "type": "object",
        "additionalProperties": {
          "type": "string",
          "minLength": 1,
          "pattern": "\\S"
        }
      }
    },
    {
      "name": "tone",
      "group": "context",
      "type": "string",
      "default": "plain and direct",
      "does": "Style instruction for anything written.",
      "sheet": "only if the sheet asks for a voice — otherwise default",
      "shape": "tone: \"<their voice, if the sheet asks for one>\"",
      "validation": {
        "type": "string",
        "minLength": 1,
        "pattern": "\\S"
      }
    },
    {
      "name": "people",
      "group": "people",
      "type": "list<object>",
      "default": null,
      "does": "Name, role, what each owns. Name them on output; never contact anyone.",
      "sheet": "Who does it today — each name and what they own, as written; no role the sheet did not state",
      "shape": "people:\n  - { name: \"<name>\", role: \"<role, only if stated>\", owns: \"<what they own, as written>\" }",
      "validation": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "name": {
              "type": "string",
              "minLength": 1,
              "pattern": "\\S"
            },
            "role": {
              "type": "string",
              "minLength": 1,
              "pattern": "\\S"
            },
            "owns": {
              "type": "string",
              "minLength": 1,
              "pattern": "\\S"
            }
          },
          "required": [
            "name",
            "owns"
          ],
          "additionalProperties": false
        }
      }
    },
    {
      "name": "rule_authority",
      "group": "people",
      "type": "list<string>",
      "default": null,
      "does": "Recorded owner for changes to a workflow rule. This is not authentication or access control.",
      "sheet": "only an explicit rule-change owner, with any topic restriction preserved; if a bare name would broaden limited authority, keep the exact scoped requirement in rules instead",
      "shape": "rule_authority: [\"<who may change a rule>\"]",
      "validation": {
        "type": "array",
        "items": {
          "type": "string",
          "minLength": 1,
          "pattern": "\\S"
        }
      }
    },
    {
      "name": "escalate_to",
      "group": "people",
      "type": "list<string>",
      "default": null,
      "does": "Escalation chain, in order. Stop at the first person who can decide.",
      "sheet": "one actual escalation chain, in order; keep separate conditional routes verbatim in rules instead of flattening them into one chain",
      "shape": "escalate_to: [\"<first>\", \"<then>\"]          # one actual ordered chain; keep separate conditional routes in rules",
      "validation": {
        "type": "array",
        "items": {
          "type": "string",
          "minLength": 1,
          "pattern": "\\S"
        }
      }
    },
    {
      "name": "inputs",
      "group": "material",
      "type": "list<object>",
      "default": null,
      "star": true,
      "does": "What arrives, from where, and which facts it reliably carries. Prefer a named input over asking someone to paste.",
      "sheet": "Task (what starts it) and Systems — what arrives and from where",
      "shape": "inputs:\n  - { name: \"<what arrives>\", from: \"<where>\", provides: [\"<what it reliably carries>\"] }",
      "validation": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "name": {
              "type": "string",
              "minLength": 1,
              "pattern": "\\S"
            },
            "from": {
              "type": "string",
              "minLength": 1,
              "pattern": "\\S"
            },
            "provides": {
              "type": "array",
              "items": {
                "type": "string",
                "minLength": 1,
                "pattern": "\\S"
              }
            }
          },
          "required": [
            "name",
            "from"
          ],
          "additionalProperties": false
        }
      }
    },
    {
      "name": "sources",
      "group": "material",
      "type": "list<object>",
      "default": null,
      "star": true,
      "does": "Where to read from when the host actually has access. Set authoritative: true only when the client evidence explicitly grants that source priority. Apply an unambiguous priority and report the disagreement; otherwise ask.",
      "sheet": "Systems / files it touches — every system named; `authoritative` only if the sheet says which wins",
      "shape": "sources:\n  - { path: \"<system or folder>\", holds: \"<what>\", authoritative: true }   # authoritative only if the sheet says it wins",
      "validation": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "path": {
              "type": "string",
              "minLength": 1,
              "pattern": "\\S"
            },
            "holds": {
              "type": "string",
              "minLength": 1,
              "pattern": "\\S"
            },
            "authoritative": {
              "type": "boolean"
            }
          },
          "required": [
            "path",
            "holds"
          ],
          "additionalProperties": false
        }
      }
    },
    {
      "name": "rules",
      "group": "work",
      "type": "list<object>",
      "default": null,
      "star": true,
      "does": "What must always hold, in the client's own words. Each may carry `because` and `beats` to settle a conflict with another rule. A rule is never traded away for speed or a deadline.",
      "sheet": "standing requirements in the client’s words; exclude workload snapshots, effort estimates, ages and temporary setup choices unless an explicit limit or quota makes them a rule. Preserve requirements without a dedicated field here verbatim, and name any missing capability",
      "shape": "rules:\n  - { rule: \"<always / never / must, verbatim>\", because: \"<their reason, if given>\", beats: \"<the rule it overrides, if stated>\" }",
      "validation": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "rule": {
              "type": "string",
              "minLength": 1,
              "pattern": "\\S"
            },
            "because": {
              "type": "string",
              "minLength": 1,
              "pattern": "\\S"
            },
            "beats": {
              "type": "string",
              "minLength": 1,
              "pattern": "\\S"
            }
          },
          "required": [
            "rule"
          ],
          "additionalProperties": false
        }
      }
    },
    {
      "name": "verify",
      "group": "work",
      "type": "list<object>",
      "default": null,
      "star": true,
      "does": "What is checked against what, and how exactly it must match. If a check cannot be run, say so — never report unchecked work as checked.",
      "sheet": "\"has a right answer in X\" — what is checked against what",
      "shape": "verify:\n  - { check: \"<what>\", against: \"<which source>\", match: \"<how exactly, if stated>\" }",
      "validation": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "check": {
              "type": "string",
              "minLength": 1,
              "pattern": "\\S"
            },
            "against": {
              "type": "string",
              "minLength": 1,
              "pattern": "\\S"
            },
            "match": {
              "type": "string",
              "minLength": 1,
              "pattern": "\\S"
            }
          },
          "required": [
            "check",
            "against"
          ],
          "additionalProperties": false
        }
      }
    },
    {
      "name": "for_each",
      "group": "work",
      "type": "string",
      "default": null,
      "does": "The thing the work repeats over — one per client, lease, household. Each run is independent; one failure does not abandon the rest.",
      "sheet": "How often / how many — the thing the work repeats over (client, lease, household)",
      "shape": "for_each: \"<the thing the work repeats over>\"",
      "validation": {
        "type": "string",
        "minLength": 1,
        "pattern": "\\S"
      }
    },
    {
      "name": "never_decide",
      "group": "guardrails",
      "type": "list<string>",
      "default": null,
      "star": true,
      "does": "Judgements to hand back to a person, even when the answer looks obvious and even when asked directly.",
      "sheet": "the judgment column — whatever must stay with a person, in their words",
      "shape": "never_decide:\n  - \"<judgement that stays with a person, verbatim>\"",
      "validation": {
        "type": "array",
        "items": {
          "type": "string",
          "minLength": 1,
          "pattern": "\\S"
        }
      }
    },
    {
      "name": "never_produce",
      "group": "guardrails",
      "type": "list<string>",
      "default": null,
      "does": "Kinds of output never to generate at all — a recommendation, a projection, a forecast. Different from a judgement: refusing to decide is not enough if the artefact itself is forbidden.",
      "sheet": "anything the sheet says the tool must never generate",
      "shape": "never_produce:\n  - \"<kind of output never to generate, verbatim>\"",
      "validation": {
        "type": "array",
        "items": {
          "type": "string",
          "minLength": 1,
          "pattern": "\\S"
        }
      }
    },
    {
      "name": "protected",
      "group": "guardrails",
      "type": "list<string>",
      "default": null,
      "does": "Content that may be filled around but never altered, reworded or reformatted.",
      "sheet": "anything that may not be reworded",
      "shape": "protected:\n  - \"<content never to reword>\"",
      "validation": {
        "type": "array",
        "items": {
          "type": "string",
          "minLength": 1,
          "pattern": "\\S"
        }
      }
    },
    {
      "name": "confidential",
      "group": "guardrails",
      "type": "list<string>",
      "default": null,
      "does": "Must not appear in anything written.",
      "sheet": "unconditional exclusions from all output; preserve conditional disclosure limits verbatim in rules rather than broadening them",
      "shape": "confidential:\n  - \"<what must not appear in output>\"",
      "validation": {
        "type": "array",
        "items": {
          "type": "string",
          "minLength": 1,
          "pattern": "\\S"
        }
      }
    },
    {
      "name": "review_required",
      "group": "review",
      "type": "boolean",
      "default": true,
      "does": "Mark the result as needing review and name who must check it. Never send, file, publish, or call anything final or approved.",
      "sheet": "true when any review wording exists",
      "shape": "review_required: true",
      "validation": {
        "type": "boolean"
      }
    },
    {
      "name": "reviewers",
      "group": "review",
      "type": "list<object>",
      "default": null,
      "star": true,
      "does": "Who checks what, and whether it is required by an outside body rather than internal preference. State that reason on the output.",
      "sheet": "review wording, usually hidden in the Task column — who checks what, and whether an outside body requires it",
      "shape": "reviewers:\n  - { who: \"<name>\", checks: \"<what>\", required_by: \"<outside body, if the sheet says so>\" }",
      "validation": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "who": {
              "type": "string",
              "minLength": 1,
              "pattern": "\\S"
            },
            "checks": {
              "type": "string",
              "minLength": 1,
              "pattern": "\\S"
            },
            "required_by": {
              "type": "string",
              "minLength": 1,
              "pattern": "\\S"
            }
          },
          "required": [
            "who",
            "checks"
          ],
          "additionalProperties": false
        }
      }
    },
    {
      "name": "destination",
      "group": "output",
      "type": "list<object>",
      "default": [
        {
          "to": "talyx-output",
          "kind": "folder"
        }
      ],
      "does": "Preferred output location: a folder, a system or a submission destination. Folder paths must stay inside the working folder. Default: local talyx-output folder. A destination is not permission to send. Report saved, delivered or confirmed only when an actual tool result establishes it; a receipt preference alone proves nothing.",
      "sheet": "Task — where it ends: a portal, a folder, a system; `receipt` when a confirmation is named",
      "shape": "destination:\n  - { to: \"<folder, system or portal>\", kind: \"<folder | system | submission>\", receipt: \"<what proves it arrived, if named>\" }",
      "validation": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "to": {
              "type": "string",
              "minLength": 1,
              "pattern": "\\S"
            },
            "kind": {
              "type": "string",
              "minLength": 1,
              "description": "Use folder for a local directory; system or submission for external destinations. Existing descriptive labels remain labels and grant no action permission.",
              "pattern": "\\S"
            },
            "receipt": {
              "type": "string",
              "minLength": 1,
              "pattern": "\\S"
            }
          },
          "required": [
            "to",
            "kind"
          ],
          "additionalProperties": false
        }
      },
      "default_label": "talyx-output (folder)"
    },
    {
      "name": "date_format",
      "group": "output",
      "type": "string",
      "default": "YYYY-MM-DD",
      "does": "How dates are written.",
      "sheet": "only if the sheet shows one",
      "shape": "date_format: \"YYYY-MM-DD\"",
      "validation": {
        "type": "string",
        "minLength": 1,
        "pattern": "\\S"
      }
    },
    {
      "name": "deadline",
      "group": "timing",
      "type": "list<object>",
      "default": null,
      "does": "When work is due, who sets it, and the consequence of missing it. A deadline never justifies skipping a check or a review — report the risk instead.",
      "sheet": "a due date and what happens if it is missed, wherever it appears; `set_by` when stated",
      "shape": "deadline:\n  - { what: \"<what is due>\", when: \"<when>\", set_by: \"<who or what sets it>\", consequence: \"<what happens if missed>\" }",
      "validation": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "what": {
              "type": "string",
              "minLength": 1,
              "pattern": "\\S"
            },
            "when": {
              "type": "string",
              "minLength": 1,
              "pattern": "\\S"
            },
            "set_by": {
              "type": "string",
              "minLength": 1,
              "pattern": "\\S"
            },
            "consequence": {
              "type": "string",
              "minLength": 1,
              "pattern": "\\S"
            }
          },
          "required": [
            "what",
            "when"
          ],
          "additionalProperties": false
        }
      }
    },
    {
      "name": "on_missing_input",
      "group": "stuck",
      "type": "ask | note",
      "default": "ask",
      "does": "A required fact is missing. ask stops and asks; note continues and marks the gap plainly.",
      "sheet": "only if the sheet says what to do — otherwise default",
      "shape": "on_missing_input: ask     # ask | note",
      "validation": {
        "type": "string",
        "enum": [
          "ask",
          "note"
        ]
      }
    },
    {
      "name": "on_conflict",
      "group": "stuck",
      "type": "ask | note",
      "default": "ask",
      "does": "Two sources disagree and none is authoritative. ask stops and asks; note records both and flags it, resolving nothing.",
      "sheet": "only if the sheet says what to do — otherwise default",
      "shape": "on_conflict: ask          # ask | note",
      "validation": {
        "type": "string",
        "enum": [
          "ask",
          "note"
        ]
      }
    },
    {
      "name": "on_check_failed",
      "group": "stuck",
      "type": "stop | note",
      "default": "stop",
      "does": "A check in `verify` did not pass. stop refuses to produce the result; note produces it marked failed. Never adjust a value to make a check pass.",
      "sheet": "only if the sheet says what to do (\"if it won't reconcile, stop\") — otherwise default",
      "shape": "on_check_failed: stop     # stop | note",
      "validation": {
        "type": "string",
        "enum": [
          "stop",
          "note"
        ]
      }
    },
    {
      "name": "on_stale_source",
      "group": "stuck",
      "type": "warn | stop",
      "default": "warn",
      "does": "A source looks out of date. warn uses it and says so; stop does not use it.",
      "sheet": "only if the sheet says what to do — otherwise default",
      "shape": "on_stale_source: warn     # warn | stop",
      "validation": {
        "type": "string",
        "enum": [
          "warn",
          "stop"
        ]
      }
    }
  ],
  "setup_questions": [
    {
      "id": "Q_FOLDER",
      "prompt": "Where should Talyx save your settings? Choose an existing working folder, or give a new folder name and where to create it.",
      "fields": [],
      "ask_when": "Only when no current or previously chosen working folder is established and local setup can save a config. Reuse an unambiguous folder answer; ask only for a missing location, never whether its name means a test.",
      "choices": "Use the host's actual folder picker when available, or accept a path. For a new folder, obtain access to its existing parent if needed, create it using available tools, and verify it. Request manual creation only after establishing the specific capability or access blocker. The working folder is not automatically a data source."
    },
    {
      "id": "Q_WORK",
      "prompt": "What recurring task would you like help with, and what should it produce?",
      "fields": [],
      "ask_when": "A named task’s intended result or the scope of a candidate-specific setting is unclear and changes what should be saved. Multiple candidate rows alone do not require selecting a workflow; general company preferences can remain general. Skip when scope and result are already clear or during an unrelated targeted update.",
      "choices": "Free text, or concrete tasks the user already named. Use the answer to target questions; do not invent a workflow-name or outcome config field.",
      "if_multiple_candidates": "If candidate-specific requirements cannot be saved without conflating their scope, ask whether to record shared company preferences or configure a named candidate first. Use only candidates in the material, including notes outside the main rows. Never select the fullest row by assumption or apply its settings to the others."
    },
    {
      "id": "Q_CONTEXT",
      "prompt": "What does your company do, who does it serve, and what work are you responsible for?",
      "fields": [
        "org_name",
        "org_description",
        "people"
      ],
      "ask_when": "During full setup when context needed for the intended work is missing. A detailed worksheet describing the company’s tasks and responsible people can supply sufficient context; do not require a generic customer-profile interview when it would not affect this work. With no such material, ask the missing company and responsibility questions. Skip unrelated context during a targeted update.",
      "choices": "Free text, existing company material, or explicitly defer. Record only stated business, customers, names, roles and responsibilities; do not infer missing people properties."
    },
    {
      "id": "Q_INPUT",
      "prompt": "Which services or folders should Talyx use, and what material should it find in each?",
      "fields": [
        "inputs",
        "sources"
      ],
      "ask_when": "During full setup, cover source choice, material or recurring inputs, and where to find them. A general service name needs a location or explicit broad-search choice. A named business system with clearly scoped records in the worksheet can already identify the intended source; do not demand folder links for every system. Flag any referenced but missing attachment. On targeted updates, clarify only affected sources. Files/folders only or later are valid choices.",
      "choices": "Offer actual discovered services, files/folders only, or decide later; accept another named service. Record exact source purposes. Capture reusable inputs and templates when supplied, not a particular meeting’s files or attendees. Never infer that the working folder is a source or that a visible tool is authorized.",
      "if_access_missing": "For a selected service, ask: Connect {service} through {host}, use an export/upload, or leave it for later? Use a native connection flow only when exposed, otherwise verified host instructions. Recheck actual access after authorization. Save known preferences even if connecting is deferred; never store credentials or an invented connected flag.",
      "if_location_missing": "Which folders or collections in {service} should Talyx use for {material}, or should it search everything you have authorized it to access there? Accept folder names, links, an explicit broad-search choice, or later. Store the chosen location in sources.path and its purpose in sources.holds; keep recurring named inputs in inputs. Do not guess folder IDs or imply permission beyond the host’s actual grants."
    },
    {
      "id": "Q_OUTPUT",
      "prompt": "What should Talyx’s results look like, and where should it save them?",
      "fields": [
        "rules",
        "tone",
        "terminology",
        "inputs",
        "sources",
        "destination"
      ],
      "ask_when": "During full setup when meaningful output preferences or destination remain unanswered. Prescribed forms, required wording and destinations in a worksheet already answer their parts; do not ask for an unrelated writing style. When no output guidance exists, ask and offer the local destination rather than silently skipping the choice. On targeted updates, clarify only affected output preferences.",
      "choices": "Describe the desired result or name an existing template/example; no format preference is valid. Offer this working folder or another user-named destination. If this folder is selected, use its talyx-output subfolder. Existing saved preferences resolve their parts; an explicit use-defaults or later answer resolves the remaining choice. Save recurring output requirements verbatim in rules, voice in tone, reusable templates in inputs/sources, and destination in destination. Keep the client’s stated task scope; a task-specific format is not automatically a rule for every workflow. External destinations do not authorize sending."
    },
    {
      "id": "Q_RULES",
      "prompt": "Are there instructions Talyx should always follow, or decisions you want left to a person?",
      "fields": [
        "rules",
        "verify",
        "never_decide",
        "never_produce",
        "protected",
        "confidential"
      ],
      "ask_when": "During full setup when standing instructions and human boundaries have not been addressed. Offer this once even if the user has not volunteered a restriction. Existing supplied rules resolve what they cover. Skip unrelated rule questions during a targeted update.",
      "choices": "No special rules, describe the instructions or boundaries, or decide later. Ask Q_HUMAN, Q_EXCLUDE or Q_PRESERVE only for a real ambiguity in the answer. No special rules does not disable review or other defaults; leave unsupported settings absent. Preserve exact wording and stated scope."
    },
    {
      "id": "Q_REVIEW",
      "prompt": "Who should review the results before they are used or shared?",
      "fields": [
        "reviewers",
        "review_required"
      ],
      "ask_when": "During full setup when reviewer or checks are unknown. Compare review statements across the supplied material first; a named reviewer does not resolve contradictory instructions about when review occurs. Ask Q_CONFLICT before saving disputed review settings. Reuse consistent existing answers. On targeted updates, ask only if review is affected. The user may defer; review remains required by default.",
      "choices": "Me, or someone else (ask for their name and what they review). Map Me only when the person's identity is supplied; otherwise retain the unresolved identity."
    },
    {
      "id": "Q_HUMAN",
      "prompt": "Which decisions must always stay with you or another person?",
      "fields": [
        "never_decide"
      ],
      "ask_when": "The client describes judgments needing a person but the boundary is ambiguous. Do not require a generic risk questionnaire when no material gap exists.",
      "choices": "Use the client's wording; examples may come only from their stated workflow."
    },
    {
      "id": "Q_EXCLUDE",
      "prompt": "What information must never appear in the output?",
      "fields": [
        "confidential",
        "never_produce"
      ],
      "ask_when": "A stated confidentiality or forbidden-output requirement needs clarification.",
      "choices": "Free text. Record exclusions verbatim; naming a requirement does not prove it is technically enforced."
    },
    {
      "id": "Q_PRESERVE",
      "prompt": "What wording must stay exactly as written?",
      "fields": [
        "protected",
        "terminology"
      ],
      "ask_when": "Provided material requires exact wording or terminology but does not specify it clearly.",
      "choices": "Free text or exact passages from supplied material."
    },
    {
      "id": "Q_CONFLICT",
      "prompt": "I found two different values for {field}: {existing} and {proposed}. Which should I keep?",
      "fields": [],
      "ask_when": "The supplied material contradicts itself across cells, columns or documents, or a proposed setting conflicts with an existing one without explicit replacement authority. Show both statements and ask which applies. Prioritize material conflicts over optional profile or style questions. Never silently choose the most detailed statement.",
      "choices": "Keep existing; use proposed; or supply the correct value. Name the setting in everyday language, never its config key. Silence is unresolved."
    },
    {
      "id": "Q_FIELD",
      "prompt": "For {stated_requirement}, what should {missing_detail} be?",
      "fields": [],
      "ask_when": "A stated requirement or unanswered core setup topic maps to an existing field but lacks a necessary detail. Ask for that detail in everyday language; do not show field names, question IDs or schema terminology. Do not introduce unrelated requirements.",
      "choices": "Use only evidence-backed choices allowed by that field's schema, otherwise free text."
    }
  ]
}
